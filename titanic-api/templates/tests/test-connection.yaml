apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "titanic-api.fullname" . }}-test-connection"
  labels:
    {{- include "titanic-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "titanic-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
